<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	
	<select id="getById" parameterType="string" resultType="uservo">
		select * 
		from member
		where id = #{id}
	</select>
	
	<insert id="insert" parameterType="uservo">
		insert into member(id, pw, name, addr, email, tel_home, tel_phone, gender, birthdate, status) 
			values(#{id}, 
			SHA2(#{pw}, 512), #{name}, 
			AES_ENCRYPT(#{addr}, 'cafe24'), 
			AES_ENCRYPT(#{email}, 'cafe24'), 
			AES_ENCRYPT(#{tel_home}, 'cafe24'), 
			AES_ENCRYPT(#{tel_phone}, 'cafe24'),
			#{gender}, #{birthdate}, '0')
			
		<selectKey keyProperty="no" resultType="int" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>
	
	<delete id="deleteAll">
		delete from member;
	</delete>
	
</mapper>
